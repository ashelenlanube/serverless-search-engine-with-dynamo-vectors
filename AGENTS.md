# AGENTS.md

## Commands

- `npm run lint`: Runs ESLint to lint the codebase.
- `npm run format:check`: Runs Prettier to check code formatting.
- `npm run format`: Runs Prettier to fix code formatting.

Do not install packages with `~` or `^` prefixes. Use pinned versions instead.
